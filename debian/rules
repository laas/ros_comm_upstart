#!/usr/bin/make -f
# -*- makefile -*-

export DH_ALWAYS_EXCLUDE=.git

# Uncomment this to turn on verbose mode.
export DH_VERBOSE=1

override_dh_installinit:
	dh_installinit --name=roscore

%:
	dh  $@
